/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['upload.wikimedia.org','assets.flowcvassets.com','s3.ap-south-1.amazonaws.com']
      },
};

export default nextConfig;

 